{% extends "portlet-body.htm.j2" %}
{% block portlet_body %}
<div class="table-responsive">
    <div id="model-glossary-report-table-toolbar" class='columns'>
        <table id="qna_summary_table"
               data-side-pagination="server"
               data-toggle="table"
               data-url="/api_btstptbl/projectgroup/{{ slug }}/qna"
               data-page-size="5"
               data-pagination="true"
               data-search="true"
               data-sort-name='created_at'
               data-sort-order="desc"
               >
            <thead>
                <tr>
                    <th data-field="title" data-formatter="btstrp_qna_linker" class="col-md-6"><i class="fa fa-briefcase"></i> {{_('제목')}} </th>
                    <th data-field="writer.email"><i class="fa fa-user"></i> {{_('작성자')}}</th>
                    <th data-field="created_at" data-formatter="btstrp_commonf_datetime"><i class="fa fa-calendar"></i> {{_('최초생성일자')}}</th>
                </tr>
            </thead>
        </table>
    </div>
</div>
<script>
var btstrp_qna_linker = function(value, row, _) {
    var url = Flask.url_for('board._qna_view', {post_id: row.id});
    return '<a data-ajax="true" href="' + url + '">' + value + '</a>';
};
</script>
{% endblock %}